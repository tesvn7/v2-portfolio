class ProjectModel {
    constructor( imageUrl, appUrl, githubUrl, name, description, technologies ) {
     
        this.imageUrl = imageUrl,
        this.appUrl = appUrl,
        this.githubUrl = githubUrl,
        this.name = name,
        this.description = description,
        this.technologies = technologies
      
    }
  }

  export default ProjectModel